var inputString = $("input#input").val();
function removeSpace(string){
  var noSpace = string.replace(/ /g, "");
  var splitArray = Array.from(noSpace);

  var rowLength = Math.round(Math.sqrt(noSpace.length))
   return rowLength


  // var re
  //   var six = 6
  //
  // var test =  noSpace.match(/.{1,six}/g);
  // return test


}
// var splitArray = noSpace.split('')
